*.gem
*.rbc
/.config
/coverage/
/InstalledFiles
/pkg/
/log/
/public/assets/
/spec/reports/
/spec/examples.txt
/test/tmp/
/test/version_tmp/
/tmp/
*.swp

spec/dummy/log/
spec/support/database.yml

# Used by dotenv library to load environment variables.
# .env

# Ignore Byebug command history file.
.byebug_history

## Environment normalization:
/.bundle/
/vendor/bundle
/lib/bundler/man/

# for a library or gem, you might want to ignore these files since the code is
# intended to run in multiple environments; otherwise, check them in:
# Gemfile.lock
# .ruby-version
# .ruby-gemset

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
.rvmrc

# Used by RuboCop. Remote config files pulled in from inherit_from directive.
# .rubocop-https?--*

# Credential tooling is intentionally unused (issue #58 ruling): secrets come
# from the environment (SECRET_KEY_BASE). There is no master key to decrypt
# encrypted credential files, so they must never be tracked.
/config/credentials.yml.enc
/config/master.key

/app/assets/builds/*
!/app/assets/builds/.keep

/node_modules/

.aider*
.env
