<%# Issue #133 — chromosome edit on the kit form treatment. %>
<%= render PageHeaderComponent.new(title: 'Editing chromosome',
                                   kicker: 'Chromosome',
                                   back_path: @chromosome,
                                   back_label: 'Back to chromosome') %>

<%= render 'form', chromosome: @chromosome %>
