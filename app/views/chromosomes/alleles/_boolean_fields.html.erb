<%# Issue #206 — a Boolean allele constrains nothing, so its field group is
    empty: the type select and the shared name field live in _form.html.erb.
    The partial exists to keep the type → markup map total (every allele type
    has exactly one home for its fields). %>
