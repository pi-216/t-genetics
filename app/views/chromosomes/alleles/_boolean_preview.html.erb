<%# Issue #206 — a Boolean allele has no constraint detail to preview; the
    row's name and type are the whole story. The partial exists to keep the
    type → preview map total (every allele type has exactly one home for its
    preview detail). %>
