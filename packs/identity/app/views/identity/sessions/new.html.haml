- if @user.errors.any?
  %div{ id: "error_explanation", class: "mb-4 rounded-md border border-danger/30 bg-danger/10 p-3 text-danger" }
    - @user.errors.full_messages.each do |message|
      %p= message

= render PageHeaderComponent.new(title: 'Sign in',
                                 kicker: 'Identity',
                                 subtitle: 'Access your organization’s evolution workspace.')

= form_with model: @user, url: login_path, class: "mt-6 max-w-md space-y-4" do |form|
  = render FormFieldComponent.new(input_id: 'identity_user_email', label: 'Email', required: true) do
    = form.email_field :email, required: true, autofocus: true, class: FormFieldComponent::INPUT_CLASSES
  = render FormFieldComponent.new(input_id: 'identity_user_password', label: 'Password', required: true) do
    = form.password_field :password, required: true, class: FormFieldComponent::INPUT_CLASSES
  = render ButtonComponent.new(label: 'Sign in', type: :submit, variant: :primary)